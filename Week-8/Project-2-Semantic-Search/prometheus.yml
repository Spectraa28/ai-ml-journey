global:
  scrape_interval: 5s # How often to check for new data

scrape_configs:
  - job_name: "fastapi-search-api"
    static_configs:
      - targets: ["host.docker.internal:8000"] # Points to your running API